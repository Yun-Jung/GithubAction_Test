name: 'public_docker_image_test'
on:
  workflow_dispatch

jobs:
  test-update-file:
    runs-on: ubuntu-latest
    container: flareforecast/send-email
    steps:
      #- name: Checkout files
      #  uses: actions/checkout@v2
      #  with:
      #    fetch-depth: 0
          
      - name: ls
        run: |
          echo "Current Directory"
          pwd
          echo "List Current Directory"
          echo "P"
          ls /__w/_PipelineMapping/Yun-Jung/GithubAction_Test
          echo "T"
          echo "ls /__w/_github_home"
          ls /__w/_github_home
          echo "ls /__w/_github_workflow"
          ls /__w/_github_workflow
          echo "ls /__w/_runner_file_commands"
          ls /__w/_runner_file_commands
          echo "Directory"
          ls /
          echo "ls /__e"
          ls /__e
          echo "ls /__t"
          ls /__t
      
      #- name: Login to Docker Hub
      #  uses: docker/login-action@v2
      #  with:
      #    username: ${{ secrets.DOCKERHUB_USERNAME }}
      #    password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Run container
        run: |
          docker run hello-world
